# Environment Configuration

# Anthropic Claude API Key (REQUIRED)
# Used for generating personalized wisdom and life coaching insights
# Get your API key from: https://console.anthropic.com/
# Cost: ~$0.0001 per wisdom generation (uses claude-3-haiku-20240307)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Google Gemini API Key (OPTIONAL, recommended)
# Used for generating author avatar portraits when not found in CDN
# Get your API key from: https://aistudio.google.com/app/apikey
# Cost: ~$0.01 per avatar generation (uses gemini-2.5-flash-image)
# Note: Most avatars (192) are pre-generated in CDN, so this is rarely used
GEMINI_API_KEY=your-gemini-api-key-here

# IMPORTANT: Never commit your actual .env file to version control
# Copy this file to .env and add your real API keys
